<!DOCTYPE html>
<html lang="en">

    <%- include('../partials/head.ejs') %>
<body>
    <h1>New Comment</h1>
    <form action="/comments" method="POST">
        <label> Rating 
            <input type="number" name="rating"/>
        </label>
        <label> Comment 
            <input type="string" name="content"/>
        </label></br>

        <label> Video
            <select name="stream">
                <% for (let i = 0; i < streamDreams.length; i++) { %> 
                    <option value="<%= streamDreams[i]._id %> "><%= streamDreams[i].song %> </option>
                <% } %> 
            </select>
            <input type="submit" name="Post Comment"/>
        </label>

    </form>
</body>

</html>